export const ROUTE_PATHS = Object.freeze({
  HEALTH_CHECK: '/health',
  PUBLIC: '/public',
  SIGN_IN: '/sign-in',
  MAGIC_LINK: '/magic-link',
  MAGIC_LINK_SENT: '/magic-link-sent'
})

export const PASSPORT_STRATEGY = Object.freeze({
  SUPABASE: 'supabase'
})

export const ACCESS_TOKEN_KEY = 'BATIK_ID_ACCESS'
export const REFRESH_TOKEN_KEY = 'BATIK_ID_REFRESH'
